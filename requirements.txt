# ===========================================================================
# Recon365 — Dependencias del Proyecto
# ===========================================================================
# Motor de inferencia: Ollama (instalar por separado)
# Modelo recomendado: llama3.1:8b (8 GB VRAM)
# ===========================================================================

# --- Scraping y navegación ---
playwright>=1.40.0             # Automatización de navegadores headless

# --- HTTP y comunicación con API local ---
requests>=2.31.0               # Llamadas HTTP al endpoint de Ollama
urllib3>=2.1.0                 # Gestión de conexiones HTTP

# --- Validación y serialización de datos ---
pydantic>=2.5.0                # Modelos de datos con tipado estricto

# --- Interfaz y logging ---
rich>=13.7.0                   # Logging en consola con colores y tablas

# --- Procesamiento de datos ---
# (estándar de Python: csv, json, pathlib — no requieren instalación)

# --- OSINT & Servidor Web (v2.0) ---
fastapi>=0.100.0               # Backend de la API REST
uvicorn>=0.23.0                # Servidor ASGI para FastAPI
dnspython>=2.4.0               # Resolución DNS
python-whois>=0.8.0            # Consultas WHOIS
networkx>=3.0                  # Construcción de grafos de correlación
aiohttp>=3.9.0                 # Peticiones HTTP asíncronas